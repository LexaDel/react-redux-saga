{
  "name": "react-redux-saga",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "copy": "copyfiles -f client/public/**/*.* public",
    "client": "cd client && yarn build && cd .. && yarn run copy",
    "dev": "yarn run client && nodemon server.js"
  },
  "repository": "git@github.com:LexaDel/react-redux-saga.git",
  "author": "Alexey Delezhov <lexadelejov@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "copyfiles": "^2.3.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "node-fetch": "^2.6.0"
  }
}
